# Cấu hình tĩnh để `spark-submit job.py` chạy được ngay, không cần --conf dài dòng.
# Dùng lại hive-metastore đã có (cùng cái Trino dùng cho Bronze) làm Iceberg catalog.
# Key/secret khớp default MinIO (minioadmin/minioadmin) — đổi nếu đổi MINIO_ACCESS_KEY/SECRET_KEY trong .env.
spark.sql.extensions                       org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.iceberg                  org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg.type              hive
spark.sql.catalog.iceberg.uri                thrift://hive-metastore:9083
spark.sql.catalog.iceberg.warehouse           s3a://lakehouse/silver/

spark.hadoop.fs.s3a.endpoint                http://minio:9000
spark.hadoop.fs.s3a.access.key              minioadmin
spark.hadoop.fs.s3a.secret.key              minioadmin
spark.hadoop.fs.s3a.path.style.access       true
spark.hadoop.fs.s3a.impl                    org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled  false
